# General properties
id = type
type = type
initializer = org.rapidcontext.core.type.Type

# Property array
property.0.name = initializer
property.0.description = The fully qualified Java class name used when creating an object instance (from the serialized data). Optional, defaults to a dictionary instance.
property.0.required = false
property.1.name = property
property.1.description = The array of object properties. Each property is a dictionary with keys 'name', 'description' and 'required' that specify some property meta-data. Optional, defaults to an empty array.
property.1.required = false
