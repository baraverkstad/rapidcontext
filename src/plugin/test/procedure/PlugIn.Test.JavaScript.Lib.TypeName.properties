# General properties
name = PlugIn.Test.JavaScript.Lib.TypeName
type = javascript
description = Returns type name for an object.

# Binding array
binding.0.name = code
binding.0.type = data
binding.0.value = let t = (obj === null) ? 'null' : typeof(obj);\n\
let s = Object.prototype.toString.call(obj).replace('object ', '');\n\
return [t, s].join(' ');
binding.1.name = obj
binding.1.type = argument
binding.1.description = The object to check
