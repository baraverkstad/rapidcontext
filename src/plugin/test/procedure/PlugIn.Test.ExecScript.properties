# General properties
name = PlugIn.Test.ExecScript
type = javascript
description = Executes a piece of JavaScript code and returns the results.

# Binding array
binding.0.type = data
binding.0.value = eval(js);\n\

binding.0.name = code
binding.1.type = argument
binding.1.description = The JavaScript source code to execute.
binding.1.name = js
binding.2.type = argument
binding.2.description = The optional data variable to process.
binding.2.name = data
