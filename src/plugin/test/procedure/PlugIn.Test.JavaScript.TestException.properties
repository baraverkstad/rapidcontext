# General properties
name = PlugIn.Test.JavaScript.TestException
type = javascript
description = Tests the JavaScript exception throwing.

# Binding array
binding.0.name = code
binding.0.type = data
binding.0.value = // Throw error instead of returning result\n\
throw new Error("javascript error");\n\
return null;
