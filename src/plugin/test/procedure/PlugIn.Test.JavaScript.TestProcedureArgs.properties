# General properties
name = PlugIn.Test.JavaScript.TestProcedureArgs
type = javascript
description = Checks the Java Procedure argument wrapping.

# Binding array
binding.0.name = code
binding.0.type = data
binding.0.value = var res = {\n\
\    type: metaType(obj),\n\
\    str: String(obj),\n\
\    json: JSON.stringify(obj)\n\
};\n\
return res;
binding.1.name = metaType
binding.1.type = procedure
binding.1.value = PlugIn.Test.JavaScript.Lib.MetaType
binding.2.name = obj
binding.2.type = argument
binding.2.description = The argument value to check
