<?js
    var data = obj;
    var self = this;

    data.forEach(function(example) {
        if (example.caption) {
    ?>
        <p class="code-caption"><?js= self.htmlsafe(example.caption) ?></p>
    <?js } ?>
    <pre class="hljs"><code><?js= self.htmlsafe(example.code) ?></code></pre>
<?js
    });
?>